# Components Folder

This folder contains reusable React components.

## Structure
Each component has its own folder with:
- `ComponentName.jsx` - The component logic
- `ComponentName.css` - The component styles
