/**
 * Legacy re-export — all pages importing from './components/Toast'
 * are automatically forwarded to the new UI design system.
 */
export { ToastProvider } from './ui/Toast';
export { useToast } from './ui/Toast';
